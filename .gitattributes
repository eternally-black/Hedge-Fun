# Keep LF on files that run in Linux containers — CRLF breaks shell scripts,
# Dockerfiles, and the Caddyfile when checked out on the VPS.
*.sh        text eol=lf
Dockerfile  text eol=lf
Caddyfile   text eol=lf
*.yml       text eol=lf
*.yaml      text eol=lf
