# Rebuilt in-image — and excluding them keeps host (Windows/WSL) native binaries out.
node_modules
.next
dist
build

# VCS / tooling
.git
.gitignore
.github

# Secrets — never enter the build context
.env
.env.*

# Docs / non-app assets
docs
app design
README.md

# Local-only / editor
.fablize
CLAUDE.md*
.vscode
.idea
.DS_Store
*.tsbuildinfo
next-env.d.ts
npm-debug.log*

# Deploy artifacts (not needed inside the image)
Dockerfile
.dockerignore
docker-compose.yml
deploy.sh
hedgefun_ci
hedgefun_ci.pub

# Not part of the image. mobile/ itself STAYS in the context: scripts/test-side-labels.ts imports
# ../mobile/src/format for the web/mobile drift check and next build type-checks scripts/.
mobile/node_modules
.claude
.codex
.scratch
ops
mobile/.expo
mobile/.audit-dist-*
docker-compose.override.yml
.deployed_release
.deployed_sha
